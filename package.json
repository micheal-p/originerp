{
  "name": "collarone",
  "version": "1.0.0",
  "private": true,
  "description": "Collarone — the business platform for Nigerian companies (HR, payroll, CRM, finance and more, all under one login)",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "typecheck": "npm run typecheck --workspace client",
    "test": "node test/rls_probe.mjs && node test/payroll_golden.mjs",
    "test:rls": "node test/rls_probe.mjs",
    "test:payroll": "node test/payroll_golden.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "pg": "^8.22.0"
  }
}
